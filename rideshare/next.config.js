// next.config.js
module.exports = {
  images: {
    domains: ['images.pexels.com'], // Add the external image domain here
  },
};


/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
};

module.exports = nextConfig;
